# 中文分词算法实验计划（精准修正版）
## 一、实验核心目标（严格对齐题目要求）
1. **核心任务**：Please design and implement an automatic word separationmodel. The input text must be Chinese, you should design yourmodel to obtain the word separation results of the input text.独立设计并实现一套完整的中文自动分词模型，输入为任意中文文本，输出符合语义的分词结果，**不允许仅调用第三方分词API完成核心功能**
2. **参考原则**：可基于老师提供的参考文档（FMM/BMM/HMM基础原理、代码框架）作为入门起点，但必须加入自主设计的改进点和优化思路，避免单纯复现
3. **对比原则**：允许调用jieba、pkuseg等工业级工具，仅用于**结果正确性验证和效果基准参照**，不作为实验核心内容
4. **报告重点**：突出**实验实现过程**、**算法设计思路**、**不同方法的本质差异与优劣分析**，重点强化**对分词算法本身的深度理解和个人见解**（方法为什么好/不好、设计权衡、适用场景）

## 二、实验环境与前置准备
### 1. 实验环境
- 编程语言：Python 3.8+
- 核心依赖：Python标准库（collections、time）+ numpy（仅用于HMM矩阵运算，不调用任何分词相关API）
- 对比工具：jieba（仅用于结果验证）
- 开发工具：PyCharm/Jupyter Notebook

### 2. 资源准备
- **词典构建**：
  - 基础词典：大于2000个通用高频词（从官方语料中提取）
  - 歧义测试词典：在基础词典基础上，补充15组典型交集歧义和组合歧义
  - 未登录词词典：新增30个2024-2025年常见网络新词和专业术语
- **测试语料集**：
  - 基础验证集：保留参考文档中的示例句子，用于验证算法正确性
  - 歧义测试集：20句典型歧义句（如"这个苹果不大好吃"、"我喜欢上一个人"）
  - 未登录词测试集：15句含网络新词和专业术语的句子
  - 长句测试集：10句长度超过30字的复杂句子

## 三、核心实验步骤（基础实现+自主改进+深度分析）
### 阶段1：基础算法实现（基于参考文档，理解核心原理）
#### 1. 基于词典的正向最大匹配（FMM）
- 实现要求：参考文档中的贪心匹配逻辑，独立编写代码
- 设计思考：
  - 为什么选择最长匹配原则？
  - 最长匹配长度MAX_LEN如何确定？
  - 无匹配时切分单字的合理性是什么？
- 测试验证：用参考文档中的示例句子验证，记录分词结果和运行时间

#### 2. 基于词典的逆向最大匹配（BMM）
- 实现要求：参考文档中的反向扫描逻辑，独立编写代码
- 设计思考：
  - 为什么逆向匹配有时比正向匹配效果好？
  - 中文分词中歧义的分布有什么特点？
  - 结果插入列表头部对效率有什么影响？
- 测试验证：用歧义句测试，对比FMM和BMM的结果差异

#### 3. 基于统计的简化HMM分词
- 实现要求：参考文档中的BMES标签体系和Viterbi算法，独立编写代码
- 设计思考：
  - 为什么用BMES标签体系？
  - 初始概率、转移概率、发射概率分别代表什么？
  - Viterbi算法的动态规划思想是什么？
- 测试验证：用简单句子验证，记录状态序列和分词结果

### 阶段2：自主设计改进（体现个人工作，避免单纯复现）
#### 1. 双向最大匹配算法（BiMM）
- 自主设计：在FMM和BMM基础上，设计更合理的融合策略
- 融合规则（自主设计）：
  1. 若FMM和BMM结果一致，直接输出
  2. 若结果不一致，优先选择词数更少的结果
  3. 若词数相同，选择单字数量更少的结果
  4. 若单字数量也相同，选择逆向匹配结果（统计表明逆向匹配准确率更高）
- 设计思考：
  - 为什么双向匹配能提高歧义处理能力？
  - 还有哪些更好的融合策略？
- 测试验证：用歧义测试集测试，对比BiMM与单独FMM/BMM的准确率

#### 2. HMM算法优化
- 自主改进：
  1. 基于小规模真实语料（1000句已分词句子），重新统计初始概率、转移概率和发射概率
  2. 加入加1平滑处理，解决未登录字符的零概率问题
  3. 优化Viterbi算法的空间复杂度，从O(n*4)优化到O(4)
- 设计思考：
  - 为什么简化版HMM效果差？
  - 平滑处理的本质是什么？
  - 如何用真实语料训练HMM参数？
- 测试验证：对比优化前后HMM算法在未登录词测试集上的效果

#### 3. 混合分词模型（可选，加分项）
- 自主设计：结合规则法和统计法的优势，设计分层分词架构
- **优化后的实现思路**（经用户异议修正）：
  1. **Layer0（新增）**：预处理 - 用规则提取英文/数字/日期实体，保护不被BiMM切碎
     - 根因：原Layer3正则已有能力，但Layer1先切碎英文导致失效
     - 实施：`_pre_extract_entities()` 提取span，BiMM只处理中文片段
  2. Layer1：用BiMM匹配词典中的已知词（相对FMM提升歧义处理）
  3. Layer2：对未匹配的连续单字（长度>=4），用优化后的HMM进行分词
  4. Layer3：后处理 - 合并相邻实体
- **关键改进**（2026-05-31）：
  - 新增Layer0实体预处理，解决英文词被切碎问题（DeepSeek/Citywalk/Agent/RAG完整保留）
  - OOV集一致率从0%提升至6.67%
  - 删除无效方案：1)Layer2 oov_ratio条件（永远为1.0） 2)BiMM平均词长规则（数学上不可能成立）
- 设计思考：
  - 为什么工业级分词系统都采用混合架构？
  - 规则法和统计法如何分工才能达到最优效果？
  - **执行顺序的重要性**：Layer0预处理激活了已有代码能力，体现了架构设计的价值
- 测试验证：对比混合模型与单一模型在所有测试集上的综合效果

### 阶段3：结果验证与对比（仅作为参考）
- 调用jieba对所有测试语料进行分词，作为效果基准
- 对比自研算法与jieba的结果差异，分析错误原因
- 统计各算法的运行效率，分析时间复杂度

### 阶段4：深度分析与个人见解（报告核心，重点强化）
#### 1. 各算法本质分析
- **基于词典的最大匹配算法**：
  - 本质：将分词问题转化为词典匹配问题，基于贪心策略选择最优解
  - 优点：原理简单、实现容易、速度极快（O(n)时间复杂度）、可解释性强
  - 缺点：完全依赖词典完整性、无法处理未登录词、歧义处理能力有限
  - 个人见解：最大匹配算法是中文分词的基础，其核心思想是"最长优先"，这符合人类的语言认知习惯。虽然简单，但在词典完善的情况下，能解决大部分无歧义句子的分词问题。

- **基于统计的HMM算法**：
  - 本质：将分词问题转化为序列标注问题，基于统计概率选择最优状态序列
  - 优点：不依赖完整词典、能处理部分未登录词、具备统计合理性
  - 缺点：需要大规模语料训练、长距离依赖处理能力差、可解释性弱
  - 个人见解：HMM算法引入了上下文信息，解决了纯规则法无法处理未登录词的问题。其核心是"马尔可夫假设"，即当前状态只依赖于前一个状态。虽然这个假设不符合语言的实际情况，但在实践中取得了不错的效果。

- **混合分词算法**：
  - 本质：结合规则法和统计法的优势，扬长避短
  - 优点：兼顾速度和精度、能处理大部分常见情况
  - 缺点：架构复杂、调参困难
  - 个人见解：混合架构是工业级分词系统的主流选择，因为它在速度和精度之间取得了很好的平衡。规则法处理已知词和特殊实体，统计法处理未登录词和歧义，这种分工符合中文分词的实际需求。
  - **2026-05-31优化经验**：执行顺序对功能的影响至关重要。Layer0预处理激活了已有代码能力（Layer3正则），体现了架构设计的价值——有时问题不在代码能力，而在调用顺序。

#### 2. 中文分词核心问题分析
- **歧义问题**：
  - 分类：交集歧义（如"研究生命起源"）、组合歧义（如"明天下雨"）
  - 分析：歧义是中文分词的最大难点，纯规则法只能解决部分歧义，统计法能利用上下文信息解决更多歧义，但仍无法解决所有歧义
  - 个人见解：歧义的本质是语言的模糊性，完全消除歧义需要语义理解，这超出了传统分词算法的能力范围。

- **未登录词问题**：
  - 分类：新词、专有名词、音译词、缩写词
  - 分析：未登录词是影响分词准确率的重要因素，纯规则法完全无法处理，统计法能通过上下文信息识别部分未登录词
  - 个人见解：未登录词识别是中文分词的研究热点，目前最好的方法是结合统计模型和深度学习模型。

#### 3. 算法设计权衡分析
- **速度vs精度**：规则法速度快但精度低，统计法精度高但速度慢，混合法在两者之间取得平衡
- **复杂度vs可解释性**：简单算法可解释性强但效果差，复杂算法效果好但可解释性弱
- **通用性vs领域适配性**：通用分词系统在各领域表现均衡，但在专业领域效果不如领域专用系统
- **规则有效性验证**（2026-05-31重要教训）：
  - BiMM平均词长规则的数学证明：当FMM和BMM词数相同时，平均词长=总字数/词数，必然相等，因此该规则永远不触发
  - Layer2 oov_ratio条件的逻辑验证：Layer2接收的文本已是连续单字（每个字都不在词典中），oov_ratio永远为1.0，条件形同虚设
  - **教训**：在添加新规则前，必须进行数学推导和逻辑验证，避免无效代码

#### 4. 实验意义总结
- 技术意义：深入理解了中文分词的核心原理和不同范式算法的设计思想
- 实践意义：掌握了从算法设计到代码实现再到优化改进的完整流程
- 学习意义：培养了独立思考和解决问题的能力，明白了算法设计中"没有最好，只有最合适"的道理

## 四、实验时间规划
| 时间 | 核心任务 |
|------|----------|
| 第1天 | 环境准备、词典和语料构建、基础原理梳理 |
| 第2天 | FMM和BMM算法实现与测试 |
| 第3天 | 简化HMM算法实现与测试 |
| 第4天 | 自主改进：BiMM算法和HMM优化 |
| 第5天 | 混合分词模型实现（可选）、结果验证 |
| 第6天 | 深度分析、报告撰写 |

## 五、实验预期成果
1. 自研分词算法完整代码（FMM、BMM、BiMM、优化后的HMM、混合模型）
2. 所有测试语料的分词结果记录表
3. 不同算法的准确率和运行效率对比数据
4. 错误类型分析报告
5. 包含深度个人见解的实验报告
6. **优化记录**（2026-05-31）：
   - Layer0实体预处理实现及效果验证（OOV集一致率0%→6.67%）
   - 无效方案删除记录（数学证明+逻辑验证）
   - 用户异议的深度分析与回应

需要我帮你整理一份**实验报告的详细大纲**，包括每个部分应该写什么内容吗？